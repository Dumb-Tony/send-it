{
  "name": "send-it", "version": "0.6.0", "private": true, "type": "module",
  "description": "SEND IT — a momentum courier platformer. First Shift campaign alpha.",
  "scripts": {"start": "node scripts/serve.mjs", "test": "node --test tests/*.test.mjs"},
  "engines": {"node": ">=20"}
}
