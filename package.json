{
  "name": "send-it", "version": "0.13.0", "private": true, "type": "module",
  "description": "SEND IT — a momentum courier platformer. Rookie Route campaign alpha.",
  "scripts": {"start": "node scripts/serve.mjs", "test": "node --test tests/*.test.mjs"},
  "engines": {"node": ">=20"}
}
