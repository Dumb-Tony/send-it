{
  "name": "send-it", "version": "0.1.0", "private": true, "type": "module",
  "description": "SEND IT — a momentum courier platformer. Milestone 1 movement laboratory.",
  "scripts": {"start": "node scripts/serve.mjs", "test": "node --test tests/*.test.mjs"},
  "engines": {"node": ">=20"}
}
